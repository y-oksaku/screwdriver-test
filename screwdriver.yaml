shared:
  image: node:20
  steps:
    - echo: echo hi
jobs:
  main:
    requires: [ ~commit, ~pr ]
  test:
    requires: [ ~tag, ~release ]
    environment:
      GIT_SHALLOW_CLONE_SINGLE_BRANCH: true
